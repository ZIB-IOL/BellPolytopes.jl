BellPolytopes.jl: sequential merge and source fixes

Merged main at f499fd9 (Fix analyticity factor with marginals), including
its preceding source audit (a2007af) and expanded unit tests (79a4f51).
Keep target compensation explicit through shrinking_target(p, eta).
Preserve the specialized branch APIs and tensor representations.

Bring the shared constructor, asymmetric-input, active-set cache, validation,
reporting, and utility fixes into this branch. Extend the convergence fix to
branch-specific heuristics: compare completed sweeps and stop when the objective
no longer decreases, instead of initially comparing it with zero. Use views for
argmin slices and tuple-valued random signs where applicable.

Branch-specific fixes
---------------------
- Preserve sequential modes, conditional strategy lengths, and physical input
  dimensions during batched conversions and active-set storage/loading.
- Fix temporary-buffer selection so ordinary bipartite mode 2 remains supported.
- Remove the duplicate o keyword; standard initialization now supplies the
  appropriate white-noise centre for either probabilities or correlations.
- Disable automatic party-permutation reduction for sequential scenarios;
  reject party-permutation expansion that would violate sequential ordering.
- Fix the wrong enumeration variable in verbose bipartite exact-oracle output.

Validation
----------
The inherited complete test suite passed: 1,766 checks (15 original checks and
1,751 audit/analyticity checks), using Julia 1.13.0 with --check-bounds=yes.
Temporary smoke checks also passed for specialized solvers, type conversions,
active-set round trips, and threshold/local-model reconstruction. Small exact
oracle comparisons and dense scalar-product checks were included where relevant.
No new branch-specific unit-test suite was added; that remains the next step.
